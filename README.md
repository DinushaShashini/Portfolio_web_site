# portfolio

## Contact form setup

1. Copy `.env.example` to `.env` and fill in your SMTP credentials.
2. Install dependencies with `npm install`.
3. Start the site with `npm start`.
4. Open `http://localhost:3000/`.

The contact form now posts to the local `/api/contact` endpoint, which sends real email through your SMTP server.

